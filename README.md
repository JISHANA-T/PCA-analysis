# PCA-analysis
Principal Component Analysis (PCA) is a statistical technique used for dimensionality reduction.
It transforms a large set of variables into a smaller set that still contains most of the important information from the original dataset.
